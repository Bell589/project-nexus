import{api}from"../api/client";import type{Character}from"../types/models";import{Panel}from"../components/Panel";
export function DevToolsPage({character,onUpdated}:{character:Character;onUpdated:(c:Character)=>void}){return <Panel title="DEV / Testfunktionen"><p><b>Nur Entwicklung:</b> Diese Buttons gehören nicht zum normalen Gameplay.</p><button onClick={()=>api.devGrant(character.id,{gold:5000}).then(onUpdated)}>+5000 Gold</button> <button onClick={()=>api.devGrant(character.id,{skillPoints:20}).then(onUpdated)}>+20 Skillpunkte</button> <button onClick={()=>api.devGrant(character.id,{statPoints:20}).then(onUpdated)}>+20 Statpunkte</button> <button onClick={()=>api.devGrant(character.id,{energy:99999}).then(onUpdated)}>Energie auffüllen</button></Panel>}
