import{useEffect,useState}from"react";import{api}from"../api/client";import type{Character,Item}from"../types/models";import{Panel}from"../components/Panel";
export function ShopPage({character,onUpdated}:{character:Character;onUpdated:(c:Character)=>void}){const[items,setItems]=useState<Item[]>([]),[err,setErr]=useState("");useEffect(()=>{api.getShopItems().then(setItems).catch(e=>setErr(e.message))},[]);return <div><Panel title="NPC-Shop"><p>Gold: <b>{character.gold}</b>. Außergewöhnliche Kräfte werden hier nicht verkauft.</p>{items.map(i=><div key={i.id} style={{borderTop:"1px solid #ddd",padding:8}}><b>{i.name}</b> — {i.description}<br/>{i.price??100} Gold · {i.rarity??"common"} <button onClick={()=>api.buyShopItem(character.id,i.id).then(onUpdated)}>Kaufen</button></div>)}</Panel>{err&&<p style={{color:"crimson"}}>{err}</p>}</div>}
